## Learned User Preferences

- Short replies **go** / **ok** / **b** advance Executor work and mark Planner or human verification on Track B milestones in the scratchpad (**b** = approve brainstorm approach). For multi-task implementation plans, prefer **subagent-driven execution** (one task per subagent; human **go** between tasks).
- Patron-facing product name is **Sense** (`apps/web/src/lib/app-brand.ts`); monorepo packages stay **`@still/*`** until post-launch internal rename (phased rebrand). Product direction: **social identity platform for taste** (not “better Letterboxd”) with a **Letterboxd-class diary and social layer** and a **Mobbin-informed web design system** (usable controls on top of cinematic chrome), not replacing the theater identity wholesale. When researching UI patterns via Mobbin, prefer **`image_format: "jpg"`** for `search_screens` because WebP responses can fail to decode in some agent or tooling environments.
- On **`/home`**, keep **sticky browse + search** (`HomeStickyChrome`), **remember catalogue chips** (sort, venue, movies vs TV, TV **`run`**) across navigation, and **prompt for watch region** on first use (**All countries** optional); **Movies**: left rail **Upcoming · Latest · Popular**, venue on the right when **Upcoming**; **TV**: left **Latest · Popular** only, right **Ongoing · Completed · Upcoming** as mutually exclusive **`?run=`** slices (not combined with each other; legacy **`?sort=ongoing|upcoming`** maps to **`run`**); **In cinemas / At home** on TV only when **`run=upcoming`**; **Continue watching** is the signed-in **`tv_watch`** rail on **`browse=tv` only** (not Movies), distinct from public **Ongoing** (TMDb **Returning Series**, not `on_the_air` — avoids overlap with **Completed**); **movie detail** browse-back restores the last catalogue context (movies vs TV label and filters), not always default **Movies** / **`/home`**; **browse rail** (**Movies · TV · Community**) must move the active pill on tap and keep chrome interactive while catalogue RSC loads — no full-page freeze; **Community** core (lists·reviews·feed) paints before **Film/TV ranks** (leaderboards client-deferred). **Mobile catalogue filter row** (`HomeLobbyFilterRow`): **one line** on narrow viewports — leading sort/feed rail **horizontal scroll** with edge fades; chips inside each pill track stay **`flex-nowrap`** (never wrap mid-pill); filters icon inside **`HOME_LOBBY_CHIP_TRACK_CLASSNAME`** (`rounded-full bg-background p-1`) like sort chips; **In cinemas / At home** on mobile lives in **`HomeCatalogFiltersPopover`** (**Release window** section), not a separate venue popover; trailing **run** / **period** via **`HomeLobbyChipPopover`** below **`sm`** when inline rails won't fit; **`sm+`** keeps full inline **`SegmentedPillToolbar`** rails. **HomeTasteMatchedHero** (signed-in **movies**): lobby media via **`HomeLobbyTasteTrailerBackground`** on **`HomeLobbyCatalogueSection`** — still **backdrop** full-bleed under trailer at the **lobby card** shell (not a nested rounded tile); TMDb **trailer iframe** when available; **movie logo** not title text; server must **enrich trailer + logo** when catalogue **`tmdbJson`** lacks **`videos`**/images; horizontal poster rail flush **right** with per-tile **opacity** fade (no x-axis scrim), **Plus** icon Quick Log, long bottom scrim/progressive blur to **`bg-card`** at the filter row — layout tokens **`home-taste-hero-layout.ts`**; **`min-[2000px]`** vertical nudge only; mobile centered stack + icon-only **Not interested**; hero pool **watchlist + diary**. **Perceived performance** is a standing priority on **`/home`**, **profile**, and Community ranks — avoid sluggish first paint.
- **Theaters** means **in-cinema** availability and **Streaming** means **watch at home**; venue filters and TMDB-backed lists should preserve that distinction with strict **Upcoming** vs **Latest** separation (including no unreleased titles in **Latest**).
- When **`browse=community`**, secondary controls are **Lists**, **Reviews**, **Activity**, **Film ranks**, and **TV ranks** on the **left** of the filter row; **Week · Month · Year · All time** on the **right** (`HomeCommunityPeriodToolbar`, same row as Movies/TV venue rail) — **`?period=`** filters every Community tab (lists by `updatedAt`, reviews by `publishedAt`, activity feed timestamps, ranks by log count). On **Film ranks** / **TV ranks**, the **Films · Shows** rank-kind toolbar sits in the **center** of `HomeLobbyFilterRow` (`center` prop on `sm+`), not the trailing rail — **Shows** means TV-series diary logs (avoid bare **TV**, the device); mobile keeps Films/Shows in the filters popover. **Activity** may inject **feed divergence** rows when ≥2 followed patrons rated the same title with **≥4.0** spread on the 0–10 scale — **`ActivityDivergenceRow`** with **Weigh in** (Quick Log); signed-in **`GET /api/feed`** only. **Film ranks** / **TV ranks** use a tier-card podium + list from #4; podium tiles show **display name** (semibold) + **@handle**; tap **@handle** → profile, tap **count** → **`PatronWatchLedgerDrawer`** with diary-style order chips (**Latest seen · Earliest seen · By title**), **Rewatch** scrim labels, and watch ordinals (**2nd watch · 3× in period**); **Members** rank contribution ledger (`PatronMembersLedgerPanel`) uses **`PatronMembersLedgerOrderChips`** — on **Reviews**/**Likes** ranks **Latest reviewed · Earliest reviewed** only (no **By title**); use **full-height centered empty states** when a tab has no rows. **`HomeCuratorSpotlights`** centers the patron row when there are few curators (`justify-center`; still scrolls on overflow).
- **Overlays, chrome, movie/TV detail, and search dialogs** should lean on **surface depth tokens**, not **borders, rings, shadows, or decorative outlines**; **movie detail** pages use a **`bg-card`** inner wrapper like catalogue lobbies. **`MobileTabBar`**: minimal **pill-sized** fixed nav (centered, not a full-width **`pointer-events-none`** strip), same **`rounded-full bg-background`** track + sliding **`bg-card`** active pill as home chips, **icon-only** with **`sr-only`** labels. Global typography: **`-webkit-font-smoothing: antialiased`** and **`-moz-osx-font-smoothing: grayscale`** on **`*`** in **`packages/ui/src/styles/globals.css`** **`@layer base`**. **Community feed rows** (`ActivityItem`, `ReviewCard`, friend rail) use flat **`bg-background`** tiles on **`bg-card`** — no row borders and **no box-shadow**; **`ActivityItem`** matches review width (**`max-w-2xl`**) — poster (**`FeedListingThumb`** **`layout="activity"`**: **`w-16`** **`aspect-2/3`** **`self-start`**) | byline (**`FeedPersonAvatar`** + name + inline **`FeedActivityVerb`** + time) | title | meta — **no side icon** chrome. **Scroll areas** (movie detail body, search dialog results): hide the visible scrollbar while keeping scroll so the layout does not jump when drawers open/close; drawer/search scrollports use **`contain-[paint]`**. Horizontal and vertical scroll fades should end at **`color/0`** (transparent), not semi-opaque opacity stops — opaque endpoints darken scrims on mobile. **`useSheetScrollFades`** coalesces scroll/resize to one RAF per frame and only updates state when fade visibility changes. **Software GPU** (no hardware acceleration): **`useSoftwareGpuRendering`** / **`detectSoftwareGpuRendering`** drops **`backdrop-blur`** on Vaul/search overlays — use an opaque scrim instead.
- **Patron ratings** display on a **0.0–10.0** scale (`log.rating` / `review.rating` stored as integer **tenths** 0–100); migration **`0031_log_rating_tenths_backfill`** multiplies legacy whole **1–10** rows by 10 — decode is always **`stored / 10`** via **`logRatingToDisplay`**. At the max, show **`10`** not **`10.0`** (`formatLogRatingDisplay` / **`formatPatronScoreTickerLabel`** / **`StillAnimateRatingNumber`**). **`formatLogRatingDisplay`** formats **display-scale 0–10 only** (slider readout); **`formatStoredLogRatingDisplay`** (or **`logRatingToDisplay`**) for API/DB tenths — never pass raw stored values to **`StarRating`** or **`formatLogRatingDisplay`** alone (shows bogus values like **47.5** or caps the slider at **1.0**). **`community.averageRating`** on movie/TV detail is already **0–10 display** — format with **`formatLogRatingDisplay`**, not **`logRatingToDisplay`**. After a **rated diary log**, **Add review** inherits the log score (hide rating slider; **`POST /api/reviews`** backfills from linked log). On **others'** profile filmography **and list detail**, show scores as on-poster **`posterCaption`** scrim like **`/diary`** via **`patronLogPosterCaption`** — not subtitle-only lines. **`log.liked`** on watch logs means **favorites** (heart chip label **Favorite**, **`IconHeartFilled`**), not a generic “liked” reaction. **Profile filmography exhausted footer** (`PopularMoviesInfinite` **`catalogExhaustedScope`**) uses patron name (**`Anselmo's profile`** / **`your profile`**) — not TMDb catalogue copy; watchlist uses **`your watchlist`**.
- **Patron portraits** in feed / rail / nav: **`PatronPortraitAvatar`** + **`profilePatronAvatarImageUrl(handle)`** (proxies private Vercel Blob) — not raw **`user.image`** in Next **`<Image>`**. **`ProfileTopBar`** truncates long display names on narrow viewports (ellipsis; keep handle visible). Signed-in viewer sees own **online/away** dot on every portrait surface (account menu included) — **server mirror** state (~20–25s lag), not local activity tracker; self SR labels **"You are online now"** / **"You are away"** (spec **`2026-06-30-presence-self-view-design.md`**). **Avatar account menu**: menu item text should be **16px** with **`px-5`** inset rows; primary routes (**Achievements**, **Log out**) use **semibold** weight — no **Request feature** or **Give feedback**; **palette** via compact **`AccountMenuThemePicker`** chips (persist on pick; pro themes when **`isPro`**) — full swatch grid stays in **Settings**, not a tall list in the dropdown. Row hover/active backgrounds should **inset within the dropdown padding** (not full-bleed to the popover edge). **Notification bell** UI stays **`HomeStickyChrome` only** (`HomeNotificationsMenu`) — not on movie/TV detail or profile top bars; **`NotificationsInboxProvider`** may still run app-wide for live unread on home + mobile Inbox tab. **Notification taps** with a title payload should deep-link to movie/TV detail + review reader (`notificationPayloadHref` / `buildMovieReviewHref`) — not dump patrons on a standalone notifications page when a content deep link exists. **Settings profile photo** empty state (`ProfileMediaCustomizer`): raised **`bg-card`** tile with persistent Upload + **Add photo** — not hover-only **Edit** on **`bg-background`** panels. **Settings destructive confirms** (`MeDestructiveConfirmDialog` on **`/me/settings/data`**): portal to **`document.body`** — **`SettingsFormShell`** wraps the page in one **`<form>`**; match **`ListLobbyDeleteConfirmDialog`** / **`AdultContentEnableDialog`** (**`max-w-lg`**, primary actions outside the typed-confirm block); confirm inputs **`rounded-2xl bg-background`**; danger-zone triggers **`variant="destructive"`** + **`border-transparent`** (no border).
- For **diary logging and edits**, capture **watch venue** (cinema vs at home); **default new logs to at home**. **Review composer** and **Quick Log** visibility use **`VisibilitySelect`** **`variant="pills"`** (**`SegmentedPillToolbar`**, same track as **`/home`** chips) — not a popover dropdown; **Settings** keeps the field variant. **Voice reviews** (optional ≤90s **`MediaRecorder`** attachment via **Text · Voice · Both** in composer): store on `review.audio_*`, upload through **`app/api/reviews/[id]/audio/route.ts`**; **`ReviewAudioPlayer`** is **tap-to-play only** (never autoplay) in feed, carousel, and reader — not gated by atmosphere prefs. **Sense audio (experimental)** in Settings uses nested **`preferences.audio`** (**`enabled` · `atmosphere` · `feedback`**) with legacy **`theaterAudio`** migration; feedback cues (**`reel-clack`**, prestige **`curtain-rise`**, streak **`streak-ping`** at 7/30/100 days) respect **`feedback`** — **no** like/follow chimes. **TV** uses hybrid **`tv_watch`** progress (statuses **watching · paused · abandoned · finished · rewatching**) plus **scoped diary** on `log` (**`show` / `season` / `episode`**); on **`/tv/[id]`**, **Watching** status and **By season / By episode** use **`SegmentedPillToolbar`** (same **`rounded-full bg-background`** track + sliding **`bg-card`** pill as **`/home`** chips, centered); **Quick Log** scope/season/episode pickers use **`StillPopoverSelect`** and the same capped scroll + bottom gradient scrim as **`review-composer.tsx`** (`max-h-[min(calc(92svh-11rem),640px)]`, `pb-24`, footer `z-20`). **Hero** watch count reflects whole-series rewatch total; **each season row** shows its own count via pencil + pill (**secondary** button colors) — no per-season **Edit diary** button. **Mark season complete** should create the diary log immediately (no second log prompt). **Rewatch** flows (movie, TV, season, episode): Quick Log title/copy and default filter must say **rewatch**, not first-watch **Add …** wording; rewatch logs with a rating **update the patron's existing title score** (edit in place), not a separate per-watch score. **Quick Log edit mode** only: **Remove from watched** (destructive link) → confirm dialog (`QuickLogRemoveConfirmDialog`, **`z-[250]`**) → **`DELETE /api/logs/:id`**; refreshes diary, profile, and movie/TV detail — not shown when composing a new log. **Watched on** (`LogWatchedDatePicker`): tap **month** / **year** header chips for quick-pick grids (not only chevron month step on the day grid). Venue + first-watch/rewatch in Quick Log use **`SegmentedPillToolbar`**; each **`AnimatePresence`** child needs a stable **`key`**; **`getWeekdayLabels()`** uses stable weekday ids (never **`key={label}`** on repeated **S/T** letters).
- **Movie detail**: titles use **SF Pro Rounded** (**`font-sans`**), not Fraunces **`font-display`**; before first log, **watchlist** (left) + **Add to Watched** pill; after logging, watchlist exits, pill morphs to **rewatch** circle, **Add review** is the primary pill, row stays **centered** (no **More actions** menu); hero synopsis uses TMDb **overview** (plot) via **`ListingDetailHeroSynopsis`**, not tagline — moderate **`max-w-md`**, **`text-pretty`**; truncated → hover blur + full-description drawer (review-slide pattern). Top bar **About · Community · Quotes · Streaming** — **Quotes** is its own tab (TV quotes scoped by **season + episode** pickers); patrons **upvote** catalog lines and **save** to a personal collection (**default private**, per-save **`content_visibility`** opt-in public). Saved quotes: **`/quotes`** lobby + **`ProfileSavedQuotesStrip`** on profile; patron **Suggest a quote** → staff queue → in-app notification on approve/reject. **Community score** on movie/TV detail aggregates **public diary log ratings** (one score per patron), not published reviews — **`GET /api/movies|tv/:id` → `community.averageRating` / `community.ratingsCount`** (**`averageRating`** already **0–10 display**); patron-facing labels say **Community score** / **public rating(s)** — no product brand prefix in UI copy; compact **`MovieDetailCommunityRatingHero` `variant="compact"`** laurel row under the synopsis; signed-in **engagement chips** (**Watched · Lists · Favorited · Watchlist**, always incl. **0**, abbrev + tooltip) via **`MovieDetailEngagementChips`** → tap **`MovieDetailEngagementDrawer`** (`GET /api/movies|tv/:id/engagement/...`, viewer-scoped rows, private-gap footer when chip count exceeds drawer rows); chips hidden unsigned; laurel hidden when no public ratings unless chips show; **`ListingPresenceProvider`** / **`ListingPresenceRow`** on **`MovieDetailViewShell`** corner (**`absolute top-4 left-4`**) — Redis ZSET heartbeats (`POST`/`DELETE /api/realtime/presence` ~25s), hybrid SSE **`presence.updated`** + ~20s poll fallback; stacked avatars + **`N viewing`** count; server-filtered **`viewingPatrons`** with `presenceState` (`active`|`away`, max **3**, **`+N`**), green/orange **`PatronOnlineDot`** on compact row + drawer avatars, and `preferences.privacy.presenceVisibility` visibility control (`friends` default, optional `public`) in Settings → Profile; when viewer is alone on the title, show **self avatar + dot only** (no others-viewing count until someone else joins); signed-in **`(app)`** movie/TV detail only (spec **`2026-06-16-listing-presence-design.md`** + AFK **`2026-06-16-presence-afk-status-design.md`** + self-view **`2026-06-30-presence-self-view-design.md`**); **no duplicate** large patron-score block in the Community/Reviews tab. **`MovieDetailTopBar`**: below **`sm`**, **back** + **Share** are **icon-only** square **`size-10`** pills (**`aria-label`** kept; text labels at **`sm+`**). **Hero backdrop** stills: slower hold between **blurred crossfades** (not rapid slideshow); hero + stills/backdrop rails use **Pasito** click-to-navigate steppers (**`DetailArtworkPasitoStepper`**, **`pasito/styles.css`** in root layout) and should also support **mouse grab-drag** horizontal scrolling. **Awards & festivals** (not “Premieres”): **Mubi-style** centered section with a **horizontal brand-logo row** (up to **12** entries in a **6-column**, two-row grid on large screens); each column title is the **festival/award name**; detail lines show **year on one line** and **winner/nominee achievement on the next** (no `year | detail` pipe); use **`text-balance`** on titles and award copy; leave **generous vertical gap** after **Cast & Crew**; **cast & crew arc** (`movie-cast-crew-arc`): **odd** visible slot count (single center apex), **normalized parabola** curve, row **padding** matching arc offset so outer cards don’t overlap the opposite row; keep cast/crew rows **tight** (content-sized — avoid tall `min-h` + `justify-center` spreading rows); on **mobile**, reduce visible arc slots so side cards are not clipped — drop outermost slots before shrinking card size. **Community** (movie + TV parity): **`MovieDetailReviewsCarousel`** — horizontal snap + edge fades; rating + laurel above headline in patron byline group; hover blur + **See full review** on **body only** when **`line-clamp`** actually truncates (ResizeObserver), not on every slide; spoiler-masked slides keep reveal CTA **visible without hover**; subsection shows count with **review/reviews**; **`MovieDetailStillsSection`** / **`movie-detail-stills-carousel.tsx`** — same carousel for TMDB stills **and** backdrops, full-resolution download, rounded wrappers (no separate background-count subtitle); reviews/stills share **`detail-editorial-rail-chrome.ts`** — short edge fades on mobile, **`cqw`**-aligned slide+spacer widths, tighter mobile slide gaps (**`ml-6`**), **`useDetailEditorialRailSnap`** recenters on mount/resize. **Spoiler reviews** (`containsSpoilers`): **`ReviewSpoilerGuard`** masks copy when the viewer **has not watched** the title (**`useViewerHasWatchedMovie`** — any diary log counts) on movie/TV detail **and every review body preview** (**Activity**, **`ReviewCard`**, profile/pinned/viral rails) — **`ActivityItem`** must not render raw **`review.body`**; own reviews never masked; composer spoiler choice uses **`SegmentedPillToolbar`**. **Review `@` mentions**: store inline film/TV tags as `@[Title](/movies|tv/id)` via **`review-listing-mentions.ts`**; composer **`@`** picker (caret-anchored popover); readers see **title + outbound arrow**, not a visible **`@`**. **Review reader** opens **`DetailVaulSheet`** (**`detail-vaul-drawer.ts`**, **`rounded-t-[2.25rem]`**) — not a centered dialog; patron avatar + name on the **handle row** top-left (time under name), icon actions top-right (**like · dislike · edit · delete**; no **Done**); floating chrome keeps **`bg-background/80`** scrim without hover; owners pick a TMDb backdrop **`still_slide_key`** (migration **`0021`**) as hero still above the review — the still picker must be available **in the review composer dialog** too, not only the post-publish reader drawer; **comment replies** nest under the parent in **`CommentsThread`**; drawer like/dislike should sync carousel counts without a full page refresh. **Drawer catalogue grids** (person filmography, rank ledger) use **`CataloguePosterTile`** **`surface="drawer"`** + **`hoverStacking="sheet"`** for **`RadialToolkit`** parity. Compact **From people you follow** avatar row under the score hero (`GET /api/movies|tv/:id/following-ratings` — latest diary score or favorite per followed patron, up to **8** + **+N more**; hidden when empty; copy says **people you follow**, not **friends**); public **Lists** show **by @handle ·** before meta; list title → list, **@handle** → profile (no nested anchors in the card); TV streams via **`TvDetailCommunityAsync`** + Suspense.
- **Sticky search** (`HomeStickySearch` / **`CatalogSearchDialogRoot`**): **unified ⌘K** only (legacy **`cmdk`** palette removed); **inline** films/TV/lists plus **People** (`GET /api/profiles/search`, following/mutual-first; anyone with a **public profile** is searchable) and **Suggested for you** when empty (signed in); **Friends** as a separate social product is **not defined yet** — graph is **mutual follow** only until a dedicated friends model ships; **Go to** via compact **`GoToDialogRoot`** (**⌘⇧K**, or **Go to…** row when catalog search is empty — not the full shortcut grid in ⌘K); no standalone **`/search`** page; global shortcut on **any app page** with the same expand animation as the **`/home`** pill; **`CatalogSearchDialogRoot`** stays **viewport-fixed** when the page is scrolled (not document-top anchored); search field has **no focus outline** and avoids **orange/accent** chrome; bar stays **centered** in **`HomeStickyChrome`** and **narrows earlier** on resize so it does not overlap left nav icons or the right account menu; collapsed pill wrapped in **`border-beam`** `BorderBeam` with per-theme **`searchBorderBeamColor`** in **`app-themes.ts`** (Calm/Lucid **`mono`**, Pensive **`ocean`**, Cozy **`sunset`**, Dreamy **`colorful`**) — defer theme-dependent `colorVariant` until **`next-themes`** hydrates (`themeReady`) so SSR and first client paint match (avoids BorderBeam inline-style hydration mismatch); collapsed pill **height stays stable** when token pills wrap. **`SearchTokenField`** + **Tab** pills (**studio** logo, **Films/TV**, **genre**, curated e.g. **Anime**, **lists** = signed-in user's lists only) via **`search-query-tags.ts`**, then free-text AND filters; ghost/tag suggestions commit with **Tab only** — must not block **Enter** search submit; **studio** tokens round-trip as removable pills on bar refocus like **genre**/**media** tags; **Films / TV** toggle must drive structured tag search too (sync **`media`** tag or pass listing kind into **`useCatalogueTagSearch`** / **`deriveCatalogueFilterBundle`** — not only ephemeral **`searchListingKind`**); toggle hides when a media pill is set; in-dialog filter row: **Popular · Latest** left, **Clear search** right; results **infinite-scroll** (first page in view, load more on scroll — parity with catalogue lobbies); **studio rail** (`SearchDialogStudioRail`, `SEARCH_DIALOG_STUDIO_IDS`): keep the rail **short** — only studios with a full **five** themed PNG set under **`public/studios/{slug}/{slug}_{suffix}.png`** (`search-dialog-studio-logo.ts` → **`SEARCH_DIALOG_STUDIO_ASSET_SLUGS`**; suffixes **theater · lucid · pensive · cozy · dreamy** for Calm/Lucid/Pensive/Cozy/Dreamy); resolve via **`SearchDialogStudioLogo`** / **`resolveStudioThemedLogoUrl`**, else TMDb **`logo_url`** on **`.studio-logo-chip`**; **no All** chip — re-tap selected studio clears filter; larger tiles; horizontal **`useHorizontalScrollFades`** scrims + **`data-lenis-prevent-wheel`** (no outer **`overflow-x-hidden`** clipping the rail); expand by adding assets + slug row (no **Browse all studios** route); **recent searches** rail: same horizontal scroll/fades; pills use **`.search-recent-chip`** (`--surface-studio-logo*` on **`bg-card`**) — **never** **`hover:bg-muted`** where **`muted` == `card`** (Calm); **search dialog body** scroll: **`useSheetScrollFades`** top/bottom gradient scrims on **`bg-card`**, inner **`flex-1 min-h-0 overflow-y-auto`** + **`data-lenis-prevent-wheel`**; **browse row** icons: Nucleo **`IconCinema`** · **`IconTvShows`** · **`IconPeople`** at **`size-5`** (Community browse keeps Lucide **`Users`**); hide scrollbar in dialog results; inline ghost completion for tags/genres must stay on **one baseline** (no overlap); hide **recent searches** block when empty (no **0** row). Empty-state **Movies · TV · People** browse swaps must **not resize** the dialog — remember tallest browse pane per open (**ResizeObserver** + **`browsePaneMinHeight`**), not a full viewport **`height`** lock; **People** loading uses list skeleton to avoid collapse. **Movie/TV detail**: **Share** copies URL to clipboard only; press motion on **back**, **share**, hero actions, and **save** — **not** About/Streaming **tabs**; **`bg-background`** controls on **`bg-card`** use **`DETAIL_CANVAS_ON_CARD_HOVER_CLASS`** (`detail-action-motion.ts`), not **`hover:bg-muted/*`**; **Streaming** tab uses **Mobbin-style** scrollable provider rail, **checkmarks** (not price pills), **`nucleo-flags`** on country rows. **`AppScrollToTop`**: hover **width-expand** with a **fixed trailing icon slot** and clear label–icon gap — animate **width** only, not gap/padding/scale together.
- **`/lists`** and **catalogue lobbies** (`/home` Movies/TV grids, **`/diary`**, **`/watchlist`**): personal-only poster-wall lobby like **`/home`** / diary / watchlist (**`HomeStickyChrome`** includes a **lists** shortcut); lobby posters use shared **`RadialToolkit`** (**`useRadialToolkitAnchor`**: hold **RMB**, drag to aim, release to confirm; suppress native **context menu** after release) with **Nucleo** icons — **destructive** wedge/label styling **only** on **Delete** (no **Share** slot); lobby actions: **Edit** (title + description), **Change cover** (device file picker → **`uploadListCover`**), **Delete** (confirm dialog aligned with account-leave pattern); list modals use **`APP_MODAL_OVERLAY_CLASS`** **`z-[250]`** above toolkit **`z-[200]`**; **system Favorites** (`favorites-list-sync.ts`) is always **`isRanked`** — membership only from diary hearts (`log.liked`), owners drag-reorder, new hearts append at the end (re-favorite does not move rank); legacy lists upgrade on open via **`repairFavoritesListPositions`**; **list detail** mirrors **movie detail** (`bg-card`, section nav, centered hero); **About** stacks cover/title **above** description and owner/visitor actions; owners set **custom cover** (pinned title or upload); lobby tiles show **title bottom-center** on the poster scrim; the **likes pill** (**`IconHeartFilled`** + count when >0) sits **top-right** on the poster — not stacked next to the bottom title. **Public SEO lists** live at **`/l/[id]`** (outside `(app)` — no auth wall); signed-in **`/lists/[id]`** keeps the full app shell; **Share** on public lists copies **`/l/…`**; signed-in visitors to **`/l/…`** redirect to **`/lists/[id]`**. Poster cells use **`CataloguePosterTile`** + **`buildCatalogueRadialItemSpecs`** — left click still navigates; signed-out gets **Open** + **Copy link** only; **home** adds Quick log + watchlist toggle (hydrates on open) + **Add to list** (films and TV); **diary** adds **Edit log** (film tile + TV group poster) + **Add to list**; **watchlist** adds Quick log + **Add to list** + **Remove from watchlist** (destructive). Picker meta uses split counts (`8 films · 4 shows · Private`; empty `0 titles · Private`). **Public list detail** has a heart like control (not owner); **`POST /api/lists/:id/like`** blocks private lists.

## Learned Workspace Facts

- The repo is a **monorepo**: main web app under **`apps/web`** (Next.js App Router), API under **`apps/server`**, shared UI and global styles under **`packages/ui`** (for example `packages/ui/src/styles/globals.css`). Typed HTTP via **`@still/api-client`** (Eden treaty): the exported singleton is named **`api`** — call **`api.api.<segment>`** (e.g. `api.api.profiles({ handle }).get()`), not `api.<segment>`. Browser **`/api/*`** fetches use **`stillApiOrigin()`** (not raw **`NEXT_PUBLIC_SERVER_URL`**) so Better Auth session cookies stay on the web host; **multipart uploads** in dev need a dedicated **`app/api/.../route.ts`** that forwards FormData + cookies to Elysia — generic Next rewrites drop the body (see **`app/api/import/anilist/route.ts`**, **`app/api/reviews/[id]/audio/route.ts`**, **`apiUpstreamOrigin()`**). **`/sign-in`** hero/backdrop must **full-bleed** into mobile safe areas (iOS Safari) like other routes — no dark bands above/below the canvas. If **`packages/config/`** is missing on disk (`Cannot find module 'zod'`), **`git restore packages/config/`** then **`bun install`**.
- **Track B** (design system and screen IA) is the canonical plan for navigation, tokens, search and browse primitives, and core screens; it lives in **`.cursor/scratchpad.md`** as tasks **B.1–B.7** alongside Phase 8 polish items — **B.4** shared primitives include **`SearchPillField`**, **`FilterChipRow`** / **`FilterChipLink`** / **`FilterChipButton`**, **`MovieCatalogSurfaceChips`**, and catalogue routes **`/movies/popular`**, **`/movies/upcoming`**, **`/movies/discover`** (chip hrefs and discover fetches stay aligned via **`apps/web/src/lib/discover-catalog-url.ts`**). **Elevation tokens** use an explicit ladder: **`surface-canvas`** → **`surface-raised`** → **`surface-overlay`**, mapped through shadcn-style **`--background`**, **`--card`**, and **`--popover`** for predictable depth on dark UI and **`.movie-themed`** pages.
- **Staff panel** (server routes **`apps/server/src/routes/staff.ts`** + `staff.test.ts`; permissions matrix in **`packages/auth/src/permissions.ts`**, role summaries via **`permissionSummary`** in `permission-summary.ts`): Users tab rows expand into **`staff-user-detail.tsx`** (summary, permissions, Pro toggle, **`staff-user-edit-form.tsx`**, append-only **Owner/Admin-only** notes in **`staff-user-notes.tsx`** lazy-loaded via `GET /users/:id/notes`; shared error copy `staff-error-message.ts`). **Quotes** tab — **`StaffQuotesPanel`**: patron submission **queue** (approve/reject + optional note) and direct **Publish** (**`POST /api/quotes/staff`** → **`listing_quote`** with **`source: staff`**, no submission row); import adapter must not overwrite staff/patron rows. Review moderation — **`StaffContentActions`** **mark-spoiler** / **unmark-spoiler** on reviews (same **content hide** permission gate as hide/delete). **Impersonation is Owner-only** (Admin never sees the button): better-auth's admin plugin requires **`session.impersonated_by`** in the Drizzle schema (migration **`0026`** — the better-auth CLI does **not** add admin-plugin columns for Drizzle); custom Elysia impersonate/stop routes must call `auth.api.impersonateUser` / stop with **`returnHeaders: true`** and forward **Set-Cookie** to the browser via **`apps/server/src/lib/forward-auth-set-cookies.ts`** (otherwise the session never swaps and impersonation silently no-ops); impersonating **admin-role targets** additionally needs the **`impersonate-admins`** permission (Owner only; **no outranks gate**, so Owners can impersonate peer Owners); amber **`impersonation-banner.tsx`** renders in **`(app)/layout.tsx`** from `impersonatedBy` on `ServerSession`.
- The authenticated app chrome is centralized in **`apps/web/src/components/app/app-shell.tsx`** (`AppShell`): **`main#main-content`**, shared horizontal gutters, and exports **`APP_SHELL_BOTTOM_RESERVE_CSS`** / **`appShellMainContentMinHeightStyle`** for pages that must match the nav inset (for example **`people/[id]`**). Bottom padding for `#main-content` is **`10px`** in **`packages/ui/src/styles/globals.css`** (same literal as **`APP_SHELL_BOTTOM_RESERVE_CSS`**). **`HomeStickyChrome`** must remain **sticky/fixed on scroll** on routes that use it (home, diary, watchlist, lists, profile, etc.) — regressions have dropped the header on scroll.
- After account deletion/ban/revoke, stale Better Auth session cookies can **`ERR_TOO_MANY_REDIRECTS`** loop **`/home` ⇄ `/sign-in`** — landing **`/`** must use **`authServer()`** not cookie presence; **`deleteUser` `callbackURL`** **`/signed-out`**; **`apps/web/src/app/signed-out/route.ts`** + **`clearSessionCookies()`** in **`session-cookie.ts`** must expire tokens with **`Secure`/`SameSite=None`** matching **`@still/auth`** (plain **`cookies.delete()`** leaves stale tokens); **`apps/web/src/proxy.ts`** must not redirect auth routes to **`/home`** on cookie presence alone (**`AuthSessionRedirect`** handles valid sessions). Valid sessions on **`/sign-in`** / **`/sign-up`**: **`AuthPageShell`** must keep **`AuthSessionRedirect`** mounted when hiding auth chrome (never `return null` alone — blank page); pages also **`authServer()`** + **`redirect()`** before paint. Auth field chrome must stay visible on **direct mobile load** — use **transform-only** CSS enter (`auth-page-content-enter`), not Motion **`opacity: 0`** that can stick when **`useSession()`** re-renders mid-check.
- **Sense launch stack** (canonical **`sense-media-platform-strategy.md`** + **`docs/superpowers/specs/2026-05-29-sense-product-roadmap-design.md`**; other specs under **`docs/superpowers/specs/`**): pre-launch **Tier 0** on existing **movies + TV + Community** (not anime-first pivot) — taste signature (`sense-taste-signature`, profile hero, **`/og/taste/[handle]`**), **Settings → Data** at **`/me/settings/data`** (sidebar **Imports** renamed **Data**; **`/me/settings/imports`** redirects): Letterboxd + Anilist import panels, **`GET /api/me/export`** Letterboxd-style ZIP (**3/hour**), **`DELETE /api/me/library`** clears watch data only (diary logs/ratings, **`tv_watch`**, watchlist, streaks, diary-derived badges/challenges — reviews, lists, follows, profile stay), Better Auth **`deleteUser`** with email verification via Resend (`packages/auth/src/lib/send-email.ts` — throw if exactly one of **`RESEND_API_KEY`** / **`EMAIL_FROM`** is set); Letterboxd CSV import (`POST /api/import/letterboxd` — rate-limited **3/hour per user**; **`me-letterboxd-import.tsx`** accepts **multiple CSVs** (diary + ratings, merged via `mergePickedFiles`); copy a picked **`FileList`** **synchronously** (`Array.from(files)`) before `e.target.value = ""` — `FileList` is **live** and React state updaters run deferred, so spreading it inside the updater sees an already-emptied list), **Anilist JSON import** (**SN.17.1**: `POST /api/import/anilist`, **`me-anilist-import.tsx`** — English-first TMDb TV match in **en-US + ja-JP**, not patron catalogue locale; optional Anilist GraphQL + MAL/Jikan title enrichment; **`userPreferred`/romaji/native** parsing; maps to watchlist · **`tv_watch`** · diary logs), onboarding quick-rate, editorial cold-start; Phase 0 retention funnel via **`product_event`** (migration **`0014`**, separate from badge **`event_log`**). **Tier 1** — **Achievements** tabs **Badges · Goals · Challenges** (completionist sets require **Join challenge** / `POST /api/challenges/:id/enroll`; unjoined shows **diary preview** progress only — muted bar + **From your diary — join to track this set**; after join, **Challenge progress** and badge unlock); **badge prestige** keeps volume milestones in Achievements but off profile tray; **taste overlap** + **Compare taste** on profiles (`/og/compare/...`); taste-challenge inbox rows use **Compare tastes** / **Not now** pills and **`?tasteCompare=1`** deep link; **watch streak** + shield on **own profile** (`user_streak`, migration `0012`); **list quality** (public descriptions ≥40 chars for Community/discoverability, per-title curator notes); **creator recognition** (`creator-recognition.ts`, **`HomeCuratorSpotlights`**, Community **Reviews** ranked by engagement — likes×2 + comments×3; relaxed thresholds in **`NODE_ENV=development`** unless **`CREATOR_RECOGNITION_STRICT=true`**; no demo seed scripts — use real public lists). **Tier 2 ST.1** — **SEO public lists**: indexable **`/l/[id]`** when public + described; **`GET /api/lists/sitemap`**; **`/sitemap.xml`** + **`/robots.txt`** allow **`/l/`**, block **`/lists/`**. **ST.2** — **Activity signature**: 52-week UTC diary heatmap (`activity-signature.ts`, **`GET /api/profiles/:handle/activity-signature`**, **`ProfileActivitySignature`** client-fetches via **`useProfileActivitySignature`**; weekday labels pinned left; auto-scroll to recent weeks; **`activityDateKeyFromUnknown`** for Date/ISO keys). **ST.3** — **Pinned signature reviews** (max 3): `profile.pinned_review_ids`, migration **`0013`**, **`PATCH /api/profiles/me/pins`**, **`ProfilePinnedReviewsStrip`**, **Pin to profile** in review reader. **ST.4** — **`HomeTasteMatchedRail`** on **`/home`** **`browse=movies`** (signed-in, ≥10 movie logs): **`GET /api/taste/for-you`**, RSC prefetch + dedicated skeleton (avoid late pop-in after the main grid), **`CataloguePosterTile`** + **`RadialToolkit`** parity with catalogue tiles; scorer favors **niche picks for niche diaries** (rating-weighted solo taste + social neighbors, **MMR diversity**), **Not interested** forever-excludes the title **and** downranks similar picks, **watchlist** movies are **hard-excluded** from `for-you` (reversible on remove); add from taste rail removes tile + backfills via **`handleTitleConsumed`**; logging a title should **refresh the rail without a full page reload**. **ST.5** — feed divergence on Community **Activity** (Δ ≥ 4.0 among followed patrons). Custom list covers: **`listPosterDisplayUrl`** / **`listBoardRowPosterUrl`** — never prefix blob/absolute URLs with TMDb; proxied covers on **`FeedListingThumb`** use **`unoptimized`**. **OG/Satori** (`ImageResponse`): every container needs explicit **`display: flex`** and a single text child per node. **Share previews (OG)** are **minimal + image-led** (1200×630, shared lib **`apps/web/src/lib/og/`**): **`/og/default`** Sense-only global fallback (root layout), **`/og/home`** popular #1 backdrop + centered Sense wordmark (`/` and `/home`, 1h revalidate), **`/og/title/movie|tv/[id]`** backdrop + corner Sense mark (poster fallback), **`/og/list/[id]`** list cover + Sense mark for public lists; **private profiles/lists fall back to `/og/default`** (no content leak), public profiles use **`/og/taste/[handle]`**; **Compare taste → copy link** copies the **`/compare/[viewer]/[target]`** page (OG metadata, redirects to **`/profile/[target]?tasteCompare=1`**) — not the raw image URL; **`robots.txt`** must **allow `/og/`** (disallowing it blocks crawlers from fetching preview images); **`/movies/`**, **`/tv/`**, **`/profile/`**, **`/people/`** detail routes render **without session** for crawlers via **`isShareableAppPath`** / **`public-share-shell`** so page-level **`og:image`** is not replaced by sign-in metadata; release notes live at **`/changelog`** and the **What's New** dialog version is bumped together with each changelog entry. **Sound layer (2026-06-13):** extends Phase 7 **`CinemaSoundProvider`** with **`profile-audio-preferences.ts`** (`preferences.audio` + **`streakMilestonesCelebrated`**); voice reviews via migration **`0027`** (`audio_url`, `audio_duration_ms`, `audio_mime_type`), **`vercel-blob-audio-put.ts`**, **`POST /api/reviews/:id/audio`**; UI **`review-audio-recorder.tsx`**, **`review-audio-player.tsx`**, **`streak-audio-watcher.tsx`**; spec **`docs/superpowers/specs/2026-06-13-sense-sound-layer-design.md`** — soundtrack snippets, ambient playlists, and podcast embeds stay deferred (licensing). **Letterboxd pillars roadmap (2026-06-13):** **`docs/superpowers/specs/2026-06-13-letterboxd-pillars-roadmap-design.md`** + plan **`docs/superpowers/plans/2026-06-13-letterboxd-pillars-roadmap.md`** — locked: **pinned-mix showcase** (4 film/TV/review slots), **no** detail ratings histogram, **full Journal**, **Wrapped**, **Members** directory, **streaming alerts**; Wave 0 shipped migration **`0028`** `showcase_items` + 8 **`product_event`** kinds. **Onboarding v3 (2026-06-14):** animated wizard on **`/onboarding`**; **`(app)`** entry gated on **`onboardedAt`** via **`patronNeedsOnboarding`** in **`onboarding-gate.ts`** (mid-wizard **handle** save alone must not unlock **`/home`**); email verify before taste **`POST /api/logs`**. **Favorite quotes (2026-06-14):** migration **`0032_listing_quotes`** + spec **`2026-06-14-favorite-quotes-design.md`**. **Wrapped / Year in review:** **`GET /api/me/year/:year`** + public **`GET /api/profiles/:handle/year/:year`**; eligible when **≥5** UTC-year diary logs; signed-in page **`/year/[year]`** uses **`(app)/year/[handle]`** where a **numeric** segment is the calendar year (must match public share route param name **`[handle]`**); share **`/year/[handle]/[year]`** + OG **`/og/year/[handle]/[year]`**; RSC fetch only in **`fetch-year-in-review-server.ts`** (client uses **`fetch-my-year-in-review-client.ts`** — never import **`next/headers`** from client bundles). **Month recap dialog** (spec **`2026-06-30-month-recap-dialog-design.md`**): first signed-in visit each calendar month in patron TZ, after **What's New** — one-time carousel of prior month global top-3 **films · TV · reviews**; skip empty categories; no dialog if all empty; `localStorage` seen per celebrated **`YYYY-MM`**.
- Import UI motion from **`motion/react`**, not **`framer-motion`** (package is **`motion`**); shared detail press springs live in **`apps/web/src/lib/detail-action-motion.ts`** and **`detail-motion-pressable.tsx`**. **Patron score ticker** (**`StillAnimateRatingNumber`**) and **skeleton reveals** (**`SkeletonShimmerReveal`**) use **`motion/react`** only — do not re-add **`motion-plus`** (`file:.cache/motion-plus.tgz`); it breaks Vercel **`bun install`** when the gitignored tarball is missing.
- **`/tv/[id]`** reuses movie detail shell components; server **`GET /api/tv/:id`**, seasons routes, **`tv-watch`** CRUD (`apps/server/src/routes/tv-watch.ts`, **`tv-season-cache.ts`**), **`ensureTvCached`** (returns **boolean** — skip diary/watchlist FK inserts when TMDb cache fails), and logs/watchlist rows with **`tvId`** (exactly one of **`movieId`** / **`tvId`**); web **`tv-detail-primary-actions.tsx`**, **`tv-log-scope-picker.tsx`**, **`StillPopoverSelect`**. **Lists**: **`favorites-list-sync.ts`** (ranked system Favorites, diary-synced items, **`POST /api/lists/:id/reorder`** allowed for owners), **`lists-lobby-catalogue.tsx`**, **`list-lobby-poster.tsx`**, **`lists-lobby-order.ts`**, **`list-detail-*`**, **`fetch-list-detail.ts`**, **`list-detail-hero-posters.ts`**, public route **`app/l/[id]`**, **`list-display-cover.ts`**, **`list-cover-image.ts`**, **`upload-list-cover.ts`**, **`vercel-blob-image-put.ts`**; **`canViewList`** in **`list-view-access.ts`** — private lists **404** on **`GET /api/lists/:id`** and **cover-image** for non-owners; shared **`RadialToolkit`** in **`packages/ui/src/components/radial-toolkit.tsx`**; modal stacking via **`apps/web/src/lib/app-modal-layer.ts`** (**`APP_MODAL_OVERLAY_CLASS`**). **`content_visibility`** enum (**`public` · `followers` · `friends` · `private`**, migration **`0016`**) on **`review`**, **`log`**, and **`profile.default_visibility`**.
- **DB/API gotchas:** Neon **`neon-http`** has no **`db.transaction()`** — multi-table patron deletes (e.g. **`clear-user-library.ts`**) use **sequential** deletes. Eden treaty may deserialize timestamps as **`Date`** or ISO strings — normalize via **`normalizeReviewDetailPayload`**, never **`.slice`** on unknown shapes. Stale **`apps/web/.next`** breaks **`Link`** route types — delete and rebuild. **`useSortable`**: destructure overridden ARIA keys before **`{...attributes}`** (see **`ranked-list-reorder-grid.tsx`**). **`tmdbPosterUrlFromPath`** supports **`w92`** for compact picker thumbs. Catalogue infinite-grid reset effects: derive a composite **`catalogueWaveKey`** when Biome **`useExhaustiveDependencies`** blocks overlapping primitives.
- **Realtime (2026-06-15, replaces Liveblocks Wave 0–1):** spec **`docs/superpowers/specs/2026-06-15-sense-realtime-redis-sse-design.md`** + plan **`docs/superpowers/plans/2026-06-15-sense-realtime-redis-sse.md`**. Shared contract **`@still/realtime`** (`packages/realtime/` — room IDs + Zod event types). Server **`publishRealtimeEvent`** (`apps/server/src/lib/realtime-publish.ts`) **`XADD`**s to Upstash Redis Streams after Postgres commit; web clients **`GET /api/realtime/stream`** SSE (`XREAD BLOCK`). Env: **`UPSTASH_REDIS_REST_URL`**, **`UPSTASH_REDIS_REST_TOKEN`** (optional — dev falls back to in-process **`realtime-dev-bus`** when unset) — **both** `apps/server` and `apps/web` must share the same Upstash project or SSE can connect with zero events. **Patron/listing presence** uses **`getPresenceRedis()`** — Upstash when set, else in-process **`presence-dev-store`** in non-production (`shouldUsePresenceDevStore()`); presence POST rate limit must allow **~25s** heartbeats plus **active/away** flips; **`PatronOnlineProvider`** posts heartbeat before **`/online`** poll, keeps an **optimistic self dot**, and must not wipe the map on an empty handle batch; **`isPatronPresenceHandle`** rejects auth **`user.id`** keys; listing presence must still **HTTP-heartbeat Redis** when WebSocket transport succeeds. When **`REALTIME_WORKER_URL`** is set, **merge** Worker DO occupancy with Redis for online/listing presence — empty Worker `[]` must not skip Redis (SSE heartbeats write Redis only); **`resolveOnlinePresenceForViewer`**, **`getListingPresenceSnapshotMerged`**, **`mergeVisiblePatronPresence`** in `patron-presence.ts` / `listing-presence.ts`. **`RealtimeRootProvider`** multiplexes inbox + dynamically registered rooms via **`useRegisterRealtimeRoom`**; **`NotificationsInboxProvider`** + **`InboxRealtimeSubscriber`** own global inbox unread (SSE + **60s safety poll** always on); **`ReviewRealtimeSubscriber`** and **`ListRealtimeSubscriber`** subscribe on that socket. **`list.reordered`** after **`POST /api/lists/:id/reorder`**; ranked list detail uses **`ListDetailRankedLive`** (editors) / **`ListDetailRankedFilmsLive`** (read-only). Same-browser tab sync: **`broadcastListReordered`** / **`BroadcastChannel`** when Upstash is unset. **Presence AFK (2026-06-16):** spec **`2026-06-16-presence-afk-status-design.md`** — **`PatronActivityProvider`** + **`usePatronActivityState`** derive `active`|`away` (tab hidden or ≥5 min idle); heartbeats send `activityState`; Redis HASH **`sense:presence:activity`**; **`GET /api/realtime/presence/online`** returns `presence: [{ handle, state }]`; **`PatronOnlineDot`** green **`active`** / orange **`away`** (`bg-desert-orange`) with micro-pop; **`usePatronPresenceState`** on portraits; listing presence passes server **`presenceState`**. Bun: run **`00-realtime-publish.test.ts` before** route tests that **`mock.module`** stub `./realtime-publish` (ordering pollution). Editorial detail carousels (stills, reviews): **`useDetailEditorialRailSnap`** — programmatic arrow nav must set **`allowScrollSettleRef`** or CSS snap + scroll velocity cascades through every slide.
- **Instant lobby navigation** (`useLobbyTransition`, `LobbyNavigationProvider`, `buildHomeLobbyHref`): **shipped** on **`/diary`**, **`/profile/[handle]`**, **`/watchlist`**, **Community period/feed chips**, **Movies·TV** sort·venue·run, and **`/home` browse rail** (**2026-05-29**) — optimistic **Movies · TV · Community** pill via **`HomeBrowseSurfaceProvider`** / **`HomeLobbyBodyGate`**, single root **`LobbyNavigationProvider`**, Community **core-first** RSC + **client-deferred** leaderboards (`fetch-home-leaderboards-client.ts`), hover/idle **`router.prefetch`**. **`/home`** also uses **`HomeStickyChrome`**, **`HomeCommunityLobby`**, TV **`home-catalog-run.ts`** / **`home-venue.ts`**, **`GET /api/tv/on-the-air`**. **`/profile/[handle]`** first load remains a known slow path (large diary fetch) — paginate or lazy-load filmography before stacking new profile features; profile filmography **Latest seen** filters **venue before** per-title dedup (**`profile-filmography-query.ts`**, **`profile-lobby-derive.ts`**) with **`createdAt`/`id`** tiebreak when **`watchedAt`** ties (parity with **`/diary`**). **`ProfileLobbyChrome`** chip rows **`justify-center`** on mobile (**`ProfileTabToolbar`** too); on social tabs hide empty order/venue rows so only the tab toolbar shows centered. Diary/watchlist venue on non-`/home` routes use **`<Link>`** where no TMDb provider is mounted; **diary venue chips filter in place**. Legacy logs missing **`watch_venue`** surface in **both** venue slices until edited. **Profile back** (`useProfileReturn` / `resolveProfileReturn`): never self-loop same **`/profile/[handle]`**; never back to **`/me/settings/*`** (settings has its own Profile affordance); fallback to last **`/home`** browse rail. **Settings back** (`useSettingsReturn` / `resolveSettingsReturn`, **`MeAccountTopBar`**): smart back to prior route — not hardcoded Profile; **`DetailReturnCapture`** persists route when entering profile or settings.
- **`/people/[id]`** uses **`PersonDetailViewShell`** — detail-shell parity with movie/TV (sticky **About · Filmography** tabs, `bg-card` inner shell, centered hero, share + smart back) without generalizing **`MovieDetailViewShell`** (spec **`2026-06-30-person-detail-shell-design.md`**). **Festival brand icons** in **`packages/ui/src/icons/`** (e.g. **`mtv.tsx`** from **`mtv.svg`**); recognition rows from **`movie-festival-recognition.ts`** + **`wikidata-movie-awards.ts`**, rendered in **`movie-premieres-festivals.tsx`**. **Cast & crew arc** layout math in **`movie-cast-crew-arc.ts`** (`CAST_CREW_ARC_EDGE_OFFSET_PX`, `arcVisibleSlotCount`, `reorderForCenterArc`). **Streaming** tab: **`movie-detail-streaming.tsx`**, **`movie-watch-providers.ts`**, **`nucleo-flag-by-iso.ts`**. **Movie detail About** uses fixed **`MovieDetailSectionNav`** (right rail) and **`MovieDetailBodySection`** with symmetric **`xl:px-28`** gutters for the legend. **Film/TV detail community:** detail **`community`** aggregates via **`fetch-public-diary-community-stats.ts`** (public rated logs, one per patron; TV **`logScope = 'show'`**); **`movie-following-ratings.ts`** (`fetchFollowingRatingsForMovie` / `fetchFollowingRatingsForTv`), **`movie-detail-following-ratings.tsx`**, list APIs return **`ownerHandle`**; **`MovieDetailReviewsCarousel`** **`movieId`** is **optional** (TV detail omits it today); **review + comment reactions** support mutually exclusive **like** and **dislike** (`reaction_kind` + **`dislikes_count`**, migrations **`0022`** / **`0023`**; **`POST /api/reviews|comments/:id/dislike`**); review still picker via **`review.still_slide_key`** + **`fetchReviewMovieScreenshots`**; Community **Film/TV ranks** ledger logs from **`leaderboard-query.ts`** include **`rewatch`**, **`watchIndexInPeriod`**, **`watchCountInPeriod`**; Achievements challenges catalog via cookie-forwarded **`fetchAchievementsChallengesCatalog`** (not silent Eden `.catch(() => [])` on the RSC path). New SQL migrations must be registered in **`packages/db/src/migrations/meta/_journal.json`** before **`bun run db:migrate`** — unjournaled files are skipped and can break queries (e.g. missing **`dislikes_count`** silently emptied review lists).
