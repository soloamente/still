import { MovieDetailViewLoading } from "@/components/movie/movie-detail-view-loading";

/** Route loader for TV detail — same shell as film detail (`MovieDetailViewShell`). */
export default function TvShowLoading() {
	return <MovieDetailViewLoading />;
}
