import { LobbyCatalogueLoading } from "@/components/app/lobby-catalogue-loading";

/** Route loader for `/home` — layout-matching lobby skeleton (overrides `(app)/loading.tsx`). */
export default function HomeLoading() {
	return <LobbyCatalogueLoading />;
}
